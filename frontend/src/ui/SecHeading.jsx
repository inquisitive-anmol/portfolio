import React from "react";

const SecHeading = ({ text }) => {
  return (
    <h1 className="text-white text-center mt-4 text-2xl md:text-4xl font-semibold">
      {text}
    </h1>
  );
};

export default SecHeading;
